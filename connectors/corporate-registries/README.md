# corporate-registries connector

**Status:** `contract_defined`

The executable contract is [`connector.contract.yaml`](connector.contract.yaml). This provider is retained in the full product scope but is not claimed as implemented in the Components 1–5 release.

Any implementation must use the shared connector SDK and cannot bypass grants, credential isolation, checkpoints, source-version checks, content inspection, or the evidence vault.
