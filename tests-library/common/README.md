# Common deterministic-test utilities

Only reviewed, deterministic, network-denied utilities belong here. Each utility requires bounded resources, typed inputs/outputs, versioning, and regression fixtures.
