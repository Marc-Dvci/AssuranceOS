# IAM deterministic-test contract

Tests in this domain must define population reconciliation, audit period, approved exceptions, exact result schema, code/input hashes, and golden false-positive cases.
