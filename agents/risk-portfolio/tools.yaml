schema_version: assurance.agent_tools.v1
agent_id: risk-portfolio
tools:
- name: organization.context.read
  description: organization context read
  operations:
  - invoke
  side_effect: none
  requires_human_confirmation: false
  network_route: service-mesh-only
  idempotency_required: false
- name: criteria.applicability.query
  description: criteria applicability query
  operations:
  - invoke
  side_effect: none
  requires_human_confirmation: false
  network_route: service-mesh-only
  idempotency_required: false
- name: risk.score.propose
  description: risk score propose
  operations:
  - invoke
  side_effect: none
  requires_human_confirmation: false
  network_route: service-mesh-only
  idempotency_required: false
- name: audit_plan.propose
  description: audit_plan propose
  operations:
  - invoke
  side_effect: none
  requires_human_confirmation: false
  network_route: service-mesh-only
  idempotency_required: false
- name: connector.requirements.map
  description: connector requirements map
  operations:
  - invoke
  side_effect: none
  requires_human_confirmation: false
  network_route: service-mesh-only
  idempotency_required: false
