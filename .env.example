# Local/test profile. Production validation deliberately rejects SQLite and disabled auth.
ASSURANCEOS_ENV=local
ASSURANCEOS_DATABASE_PATH=./var/assuranceos.db
# ASSURANCEOS_DATABASE_URL=postgresql+psycopg://assuranceos:password@localhost:5432/assuranceos
ASSURANCEOS_AUTO_CREATE_SCHEMA=false

ASSURANCEOS_AGENT_ROOT=./agents
ASSURANCEOS_DEMO_ROOT=./demo/asteria
ASSURANCEOS_CONTROL_TEST_ROOT=./tests-library
ASSURANCEOS_CONTROL_TEST_PUBLIC_KEY=./security/release-keys/control-test-release-public.pem
ASSURANCEOS_AUDIT_PACK_ROOT=./audit-packs
# Audit Packs are signed by their own key, separate from the agent-release key:
# different artefact classes, different authors, different review paths, so a
# compromise of one review does not carry to the other. Only the public half is
# committed; generate the pair with scripts/generate_audit_pack_signing_key.py.
ASSURANCEOS_AUDIT_PACK_PUBLIC_KEY=./security/release-keys/audit-pack-release-public.pem
# Hard memory/CPU limits need the POSIX resource interface. Windows developer machines
# have none, so the sandbox must be downgraded explicitly there. Production rejects this.
ASSURANCEOS_CONTROL_TEST_ALLOW_DEGRADED_SANDBOX=false
ASSURANCEOS_MODEL_MODE=mock
# The hackathon mandates Gemini 3.5 or newer.
ASSURANCEOS_GEMINI_MODEL=gemini-3.7-flash
# Managed Agent Engine deployment and proof. The deploy script writes the result
# document at this path; Cloud Run may instead receive the JSON through
# ASSURANCEOS_AGENT_ENGINE_RESOURCE_MAP_JSON.
ASSURANCEOS_AGENT_ENGINE_STAGING_BUCKET=gs://your-project-assuranceos-agent-staging
ASSURANCEOS_AGENT_ENGINE_PROOF=var/agent-engine-deployment-result.json
# Managed Model Armor. The template guards the request path of any deployment, so
# its proof is independent of Agent Engine: `python scripts/verify_model_armor.py`
# exercises the template on both decision paths and writes the receipt below.
# A configured template is not evidence; only a passing receipt reports verified.
ASSURANCEOS_MODEL_ARMOR_TEMPLATE=
ASSURANCEOS_MODEL_ARMOR_PROOF=var/model-armor-proof.json
# Local privacy runtime: any OpenAI-compatible endpoint. The base URL is explicit
# and there is no fallback to a hosted model, so a local deployment cannot
# silently start sending evidence to a third party.
ASSURANCEOS_LOCAL_MODEL_URL=http://127.0.0.1:5000/v1
ASSURANCEOS_LOCAL_MODEL_NAME=local
# Reasoning-model deliberation. Leave empty to use the server default. Set false
# for structured audit output: measured on gemma-4-12b-it-IQ4_XS, deliberation
# consumed a whole 4096-token ceiling and produced no answer at all, while the
# same prompt answered in 171 tokens with it off. Probe your own server first -
# several silently ignore reasoning_effort and chat_template_kwargs.
ASSURANCEOS_LOCAL_MODEL_ENABLE_THINKING=false

# EmbeddingGemma. The retrieval index over canonical evidence: how an auditor
# finds candidates, never what a claim resolves to. `deterministic` is the test
# transport and produces vectors with no meaning; it declares itself as such
# rather than passing for a working index. `local` runs EmbeddingGemma on a
# loopback llama.cpp /v1/embeddings endpoint, `vertex` runs it on Vertex AI.
ASSURANCEOS_EMBEDDING_MODE=deterministic
ASSURANCEOS_EMBEDDING_MODEL=embeddinggemma-300m
ASSURANCEOS_EMBEDDING_URL=http://127.0.0.1:5001/v1
# Matryoshka width. 768 is the trained representation; 512, 256 and 128 are
# supported truncations that trade a little recall for storage and comparison
# cost. Anything else is refused, because an untrained width does not error, it
# just retrieves worse.
ASSURANCEOS_EMBEDDING_DIMENSIONS=768

# Chirp 3. Walkthrough-interview transcription through Speech-to-Text v2. The
# recording and its transcript enter the vault as management assertions, never
# as accepted evidence. `scripted` replays a fixed transcript offline; `chirp`
# calls the model. Speech-to-Text v2 is regional and Chirp 3 is not served from
# the global endpoint, so the location is configuration.
ASSURANCEOS_SPEECH_MODE=scripted
ASSURANCEOS_SPEECH_MODEL=chirp_3
ASSURANCEOS_SPEECH_LOCATION=us-central1

ASSURANCEOS_EVIDENCE_STORAGE=local
ASSURANCEOS_EVIDENCE_ROOT=./var/evidence
ASSURANCEOS_EVIDENCE_EXPORT_ROOT=./var/evidence-exports
ASSURANCEOS_EVIDENCE_BUCKET=
ASSURANCEOS_MAX_EVIDENCE_UPLOAD_BYTES=52428800

# Local developer convenience only. Set jwt in deployed environments.
ASSURANCEOS_AUTH_MODE=disabled
ASSURANCEOS_AUTH_JWT_ISSUER=
ASSURANCEOS_AUTH_JWT_AUDIENCE=
ASSURANCEOS_AUTH_JWT_SECRET=
ASSURANCEOS_AUTH_JWKS_URL=
ASSURANCEOS_AUTH_JWT_ALGORITHMS=RS256
ASSURANCEOS_AUTH_CLOCK_LEEWAY_SECONDS=30
ASSURANCEOS_TRUSTED_HOSTS=localhost,127.0.0.1,testserver

# Optional locally; required by the production API.
ASSURANCEOS_EXPORT_SIGNING_PRIVATE_KEY=
ASSURANCEOS_EXPORT_SIGNING_PUBLIC_KEY=
ASSURANCEOS_EXPORT_SIGNING_KEY_ID=local-ed25519-v1

# Required for agent task issuance in deployed environments. The private key belongs
# to the control plane; only security/release-keys/execution-envelope-public.pem is committed.
ASSURANCEOS_EXECUTION_SIGNING_PRIVATE_KEY=
ASSURANCEOS_EXECUTION_ENVELOPE_PUBLIC_KEY=./security/release-keys/execution-envelope-public.pem
ASSURANCEOS_EXECUTION_SIGNING_KEY_ID=assuranceos-execution-v1
ASSURANCEOS_EXECUTION_ENVELOPE_TTL_SECONDS=900

GOOGLE_CLOUD_PROJECT=
# Where things are DEPLOYED: Cloud Run, Cloud SQL and Agent Engine need a region.
GOOGLE_CLOUD_LOCATION=us-central1
GOOGLE_CLOUD_STAGING_BUCKET=

# Where Gemini is CALLED, which is not the same thing. Gemini 3.x is served from
# the `global` Vertex endpoint only; ask a region for it and you get a 404 that
# reads exactly like a typo in the model id — while models.list in that same
# region cheerfully lists it. One variable could not be both values.
ASSURANCEOS_GEMINI_LOCATION=global

# Real credentials belong in .env.local, which is gitignored. This file is a
# committed template and anything set here ships with the repository.
