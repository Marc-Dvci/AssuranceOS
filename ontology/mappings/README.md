# Ontology mapping contract

Provider fields map to versioned canonical entities without overwriting source identifiers. Every mapping records source, version, effective date, and transformation lineage.
