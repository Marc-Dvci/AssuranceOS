# Local runtime contract

Docker Compose/PostgreSQL/local evidence profile with outbound-network denial and explicit LOCAL PRIVACY MODE. The cloud release does not silently fall back to this profile.
