# Gemma local reasoning profile

No model weights are committed. Release requires the exact upstream revision, GGUF digest, quantization recipe, llama.cpp commit, hardware qualification, and task-specific evaluation thresholds.
